<header>
    <div class="nav">
        <% if (!user.id || !user.login){ %>
          <p class="hello"></p>
        <% } else{ %>
          <p class="hello">hello <%= user.login %> </p>
        <% } %>
        <div class="item">
          <a href="">О клубе</a>
          <a href=""> Команда</a>
          <a href="">Программы</a>
        </div>
        <div class="item">
          <img src="/images/logo.png" alt="">
        </div>
        <div class="item">
          <a href="">Цены</a>
          <a href="">Новости</a>
          <% if (!user.id || !user.login) { %>
            <a href="/autorization">Вход/Регистрация</a>
          <% } else if (user.id == "5cc920f41717053a70a4a510") { %>
            <a href="/api/auth/logout">Выход из админки</a>
          <% } else { %>
            <a href="/api/auth/logout">Выход</a>
          <% } %>
        </div>
    </div>
</header>